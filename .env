# Docker Compose Environment Configuration
# Customize these values for your local development environment

# AlgoSvc Configuration
ALGOSVC_PORT=8080

# Optional Nginx Reverse Proxy (when using --profile with-nginx)
NGINX_PORT=80

# Java Runtime Options (already set in compose, can override here)
# JAVA_TOOL_OPTIONS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=50

# Docker Build Options
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1